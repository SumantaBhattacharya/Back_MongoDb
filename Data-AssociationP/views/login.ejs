<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Great Vibes', cursive;
        }
        .alert {
            display: none;
        }
        .alert.active {
            display: block;
        }
    </style>
</head>
<body class="bg-zinc-900 flex items-center justify-center h-screen">
    <div class="w-full max-w-sm p-8 bg-zinc-800 rounded-lg shadow-md">
        <h3 style="border-bottom: 0.2vw solid white; border-radius: 2px;" class="text-3xl mb-5 text-zinc-100">Login</h3>
        
        <% if (typeof message !== 'undefined') { %>
        <div class="alert active text-red-600 bg-red-200 p-4 rounded" id="alert-message">
            <%= message %>
        </div>
        <% } %>
        
        <form action="/login" method="post" class="space-y-6">
            <div>
                <label for="email" class="block mb-2 text-xl font-medium text-zinc-100">Email</label>
                <input type="email" id="email" placeholder="Email" name="email" class="w-full px-6 py-4 text-xl text-zinc-900 bg-zinc-100 rounded-lg focus:outline-none focus:ring-2 focus:ring-zinc-500">
            </div>
            <div>
                <label for="password" class="block mb-2 text-xl font-medium text-zinc-100">Password</label>
                <input type="password" id="password" placeholder="Password" name="password" class="w-full px-6 py-4 text-xl text-zinc-900 bg-zinc-100 rounded-lg focus:outline-none focus:ring-2 focus:ring-zinc-500">
            </div>
            <input type="submit" value="Login" class="w-full px-6 py-4 font-bold text-xl text-white bg-zinc-600 rounded-lg hover:bg-zinc-500 focus:outline-none focus:ring-2 focus:ring-zinc-500">
        </form>
    </div>

    <script>
        window.onload = function() {
            const alertMessage = document.getElementById("alert-message");
            if (alertMessage) {
                alert(alertMessage.textContent);
                setTimeout(() => {
                    window.location.href = "/login";
                }, 2000);
            }
        };
    </script>
</body>
</html>
